// app/page.tsx
// Temporary root page — will be replaced by (protected)/page.tsx in Plan 01-05

export default function RootPage() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <p className="text-muted">Setting up&hellip;</p>
    </main>
  )
}
