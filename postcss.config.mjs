// postcss.config.mjs
// Tailwind v4 PostCSS bridge — uses @tailwindcss/postcss (NOT the v3 tailwindcss plugin)
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}
export default config
